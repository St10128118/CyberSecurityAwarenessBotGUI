<Window x:Class="CyberSecurityAwarenessBotGUI.EditFactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Fact" Height="180" Width="420" WindowStartupLocation="CenterOwner">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Key" FontWeight="SemiBold"/>
            <TextBox x:Name="KeyText" IsReadOnly="True"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <TextBlock Text="Value" FontWeight="SemiBold"/>
            <TextBox x:Name="ValueText"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Save" Width="80" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Margin="8,0,0,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>